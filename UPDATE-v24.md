# Cập nhật Aqua FPL Advisor v24

Ghi đè toàn bộ mã nguồn v23 bằng nội dung gói v24 rồi deploy lại Netlify.

Không cần thay đổi Environment Variables hoặc OpenAI API key. Endpoint FPL nội bộ có thêm tác vụ tải thống kê chi tiết của các ứng viên chuyển nhượng.

Sau khi Netlify báo **Published**, mở website và tải lại mạnh bằng `Ctrl + F5`.
