# Cập nhật Aqua FPL Advisor v38

## Thay đổi

- Nội dung thống kê không còn tràn qua đường viền dưới của thẻ ứng viên trên mobile.
- Thẻ ứng viên tự giãn chiều cao và chừa khoảng cách an toàn cho dòng `F5`.
- Các dòng `L5` và `F5` chỉ cuộn bên trong thẻ khi không đủ chiều rộng.
- Ký hiệu Gameweek trong lịch sử gần nhất được đổi từ `GW1` thành `R1`.

## Cập nhật từ v37

Ghi đè toàn bộ mã nguồn bằng nội dung gói v38 rồi triển khai lại trên Netlify. Không cần thay đổi Environment Variables hoặc cấu hình API.
