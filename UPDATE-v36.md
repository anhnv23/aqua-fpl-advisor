# Cập nhật Aqua FPL Advisor v36

## Thay đổi

- `BIB` (Balance If Bought) được đặt ngay cạnh giá của ứng viên.
- Người dùng có thể thấy giá mua và ngân sách còn lại cùng lúc mà không cần trượt hàng dữ liệu.
- Giá trị âm vẫn có dấu trừ và được cảnh báo màu đỏ.
- Nhãn `BANK` đã được loại bỏ khỏi hàng thông tin phía dưới.

## Cập nhật từ v35

Ghi đè toàn bộ mã nguồn bằng nội dung gói v36 rồi triển khai lại trên Netlify. Không cần thay đổi Environment Variables hoặc cấu hình API.
