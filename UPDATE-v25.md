# Cập nhật Aqua FPL Advisor v25

Ghi đè toàn bộ mã nguồn v24 bằng nội dung gói v25 rồi deploy lại Netlify.

Không cần thay đổi Environment Variables hoặc OpenAI API key. Bản cập nhật chỉ sửa bố cục danh sách ứng viên trên mobile.

Sau khi Netlify báo **Published**, mở website và tải lại mạnh bằng `Ctrl + F5`.
