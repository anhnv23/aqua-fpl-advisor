# Cập nhật Aqua FPL Advisor v39

## Luồng chuyển nhượng trên mobile

1. Chạm cầu thủ trên sân và chọn **Chuyển nhượng**.
2. Màn hình tự cuộn xuống khu vực **Chọn cầu thủ**.
3. Chạm ứng viên muốn đưa vào đội.
4. Màn hình tự cuộn lên sân để kiểm tra đội hình sau thay đổi.

Danh sách ứng viên vẫn được giữ mở. Cơ chế cuộn chỉ chạy trên màn hình rộng tối đa 760 px.

## Cập nhật từ v38

Ghi đè toàn bộ mã nguồn bằng nội dung gói v39 rồi triển khai lại trên Netlify. Không cần thay đổi Environment Variables hoặc cấu hình API.
