# Cập nhật Aqua FPL Advisor v17

Ghi đè toàn bộ mã nguồn v16 bằng nội dung gói v17 rồi deploy lại Netlify.

Không cần thay đổi Environment Variables, OpenAI API key hoặc dữ liệu. Bản cập nhật chỉ thay đổi giao diện phía trình duyệt.

Sau khi Netlify báo **Published**, mở website và tải lại mạnh bằng `Ctrl + F5`.
